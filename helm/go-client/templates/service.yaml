apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-client.fullname" . }}
  labels:
    {{- include "go-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "go-client.selectorLabels" . | nindent 4 }}
