apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-client.fullname" . }}-test-connection"
  labels:
    {{- include "go-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
